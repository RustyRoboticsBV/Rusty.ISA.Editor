# ActionGraph Runtime

This directory contains all ActionGraph code that is relevant at runtime.

It mainly contains:
- The `InstructionProgram` class: a resource containing instructions.
- The `InstructionProcess` class: a node that can execute programs.
- The `ExecutionHandler` class: an abstract base class for implementing instruction execution handlers.
